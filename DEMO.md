# 基础战斗 Demo

## 启动

在项目目录运行：

```bash
godot --path "$(wslpath -w /mnt/d/Godot/aaa)"
```

或双击 `project.godot` 后在 Godot 中按 F6/F5。

## 操作

| 按键 | 行为 |
|------|------|
| `Space` | 架势防范：化解 +1 还愿，完美接刀 +2；按空（气息散乱）冷却 0.75 秒，落刀后 50ms 内补防仍算化解 |
| `1` | 斩纸：耗 1 点，散去 4 点怨气 |
| `2` | 还刃：耗 3 点，斩去 12 点怨气；完美接刀后乘势追加 6 |
| `3` | 镇煞：耗 2 点，斩去 6 点怨气，鬼招短暂凝滞 |
| `4` | 续灯：耗 2 点，回复 7 点灯油 |
| `R` | 重新开始 |

## 本切片验证内容

- 敌人会自动连续出招，不存在规划回合或手动结束回合
- 赤·嗔：中途停顿和假释放点的延迟慢刀
- 碧·痴：快慢变拍的二连击，需要对两次落刀分别使用同一个防范动作
- 青·疑：先以武器佯攻，再转鬼手投技，仍使用统一防范
- 普通/完美判定、失误冷却、还愿点获取、四张消费卡和基础胜负循环
- 完美弹反的 hit-stop、创伤震屏、金白闪光、径向碎片和音效
- C 手绘厚涂为唯一运行画风，敌人身体与武器使用独立动画层
- 攻防同台：三套敌招都会突进到玩家半场并统一打在执灯人身上，玩家有提灯护身、化解回弹、完美定格、受击踉跄、掷符与续灯光晕等姿态反馈
- 完美接刀会让厉鬼僵直：连击中途则下一拍延迟 0.6 秒，整招结束则收招多停顿 0.8 秒
- 不显示精确进度条或“现在按键”提示；以蓄势、回收、踏步、白刃闪光和鬼手伸出表现真实窗口
- 执灯人呼吸浮动、厉鬼悬浮、灯火闪烁、冷色鬼气、移动雨层和双层雾带

这是机制验证切片，不包含地图、商店、抽牌、牌库构筑和局外成长。

## 代码结构

参照杀戮尖塔 2 的 Model/Node 分离（见 [STS2_CODE_RESEARCH.md](STS2_CODE_RESEARCH.md)）：

- `scripts/battle/battle_simulation.gd`：纯规则层。命令进（`defend` / `play_card`），事件出（`impact` / `card_played` / `victory`……），不依赖任何场景节点。
- `scripts/main.gd`：纯表现层。只读模拟状态、消费事件，负责动画、音频、UI 与震屏 hit-stop。

## 自动验证

运行：

```bash
./tools/validate_demo.sh
```

验证分为两层：

- 无窗口烟雾测试：覆盖自动出招、统一防范、普通/完美判定、失误冷却、点数获取和卡牌消费。
- 有窗口视觉测试：通过真实 `Space` 完成三类防范并真实出牌，检查自动攻击循环、冷却按钮、独立武器、鬼手投技、卡牌边界和截图可见性。

输出位置：`validation/report.json` 与 `validation/*.png`。

当前验证基线：52 项检查全部通过，生成 6 张关键帧。检查内容包括自动连续出招、统一防范、冷却、普通/完美得点、卡牌消费、武器与鬼手动画、卡牌边界与截图非黑屏。

完整卡牌获取方案见 [CARD_ACQUISITION.md](CARD_ACQUISITION.md)。
